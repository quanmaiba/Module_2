@model IEnumerable<BoDeTracNghiemDemo.Models.DapAn>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TenDapAn)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CauHoi)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TenDapAn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CauHoi.CauTraLoi1)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.DapAnId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.DapAnId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.DapAnId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
