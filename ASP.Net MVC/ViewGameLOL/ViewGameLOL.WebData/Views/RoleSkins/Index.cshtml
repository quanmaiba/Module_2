@model IEnumerable<GameLol.DoMain.RoleSkin>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NameRoleSkin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NameRoleSkin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.RoleSkinID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.RoleSkinID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.RoleSkinID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
